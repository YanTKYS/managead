# v0.9.6 打鍵テスト結果記録

検証手順:
- `docs/operation/validation-readonly.md`
- `docs/operation/validation-computer-edit.md`（コンピュータ参照観点のみ）
- `docs/operation/validation-group-edit.md`（グループ参照・メンバー表示観点のみ）

---

## 基本情報

| 項目 | 内容 |
|---|---|
| 実施日 | 未記入（初回・2回目報告） |
| 実施者 | 未記入 |
| バージョン | v0.9.6 打鍵テスト |
| 起動モード | DirectoryReadOnly |
| OS | 未記入 |
| 実行端末 | 未記入 |
| AD 環境 | 未記入 |

---

## 初回報告の評価

結果欄は、○（成功）/ △（成功か不明・追加確認が必要）/ ✕（問題あり）/ -（未実施）で記録します。

| # | 該当手順・観点 | 結果 | 報告内容 | 次回確認事項 |
|---|---|---|---|---|
| 1 | 起動時に `DirectoryReadOnly` を選択して起動できる | ○ | `ServiceMode` を `DirectoryReadOnly` にして起動できた | v0.9.6 では起動時ダイアログで `DirectoryReadOnly` を選択できることも確認する |
| 2 | ユーザー検索（SamAccountName） | ○ | SamAccountName でユーザー検索できた | 検索結果の対象ユーザーが想定どおりであること |
| 3 | ユーザー検索（DisplayName） | ○ | DisplayName でユーザー検索できた | 表示名の部分一致・日本語名での検索も必要に応じて確認する |
| 4 | ユーザー属性表示 | ○ | 属性が表示された | v0.9.6 では「v0.4.2 更新対象外」表記が表示されないことも確認する |
| 5 | ユーザー所属グループ表示 | ○ | 所属グループが表示された | グループ名順・コピー操作は未確認のため必要に応じて追加確認する |
| 6 | コンピュータ参照 | ○ | コンピュータ参照ができた | コンピュータ詳細・所属グループ表示は必要に応じて追加確認する |
| 7 | グループ検索 | △ | グループ検索はできた | 検索結果で対象グループを選択できること |
| 8 | グループのユーザーメンバー表示 | △ | 所属ユーザーは不明 | `ユーザーメンバー` 一覧に期待するユーザーが表示されるかを再確認する |

---

## 2回目報告の評価

| # | 該当手順・観点 | 結果 | 報告内容 | 対応・次回確認事項 |
|---|---|---|---|---|
| 1 | リリース ZIP のルート直下 | ○ | exe や json などの必要ファイルだけになり、分かりやすくなった | 継続してリリース成果物で確認する |
| 2 | 起動時 ServiceMode 選択画面 | ○ | 起動時に ServiceMode の選択画面が表示された | 選択後にメイン画面へ遷移することを次項で確認する |
| 3 | ServiceMode 選択後のメイン画面遷移 | ✕ | 選択後、次の画面に遷移しない。ログにも出ず、何が起きているのか不明 | WPF の ShutdownMode をメイン画面表示まで明示終了に変更し、起動ログと起動失敗時メッセージを追加する |

---

## 3回目報告の評価

| # | 該当手順・観点 | 結果 | 報告内容 | 対応・次回確認事項 |
|---|---|---|---|---|
| 1 | ServiceMode 選択後のメイン画面遷移 | ○ | ServiceMode 選択後、メイン画面に遷移した | 継続確認 |
| 2 | ユーザー検索 UI から「部署」削除 | ○ | 入力項目と結果から「部署」が削除された | 継続確認 |
| 3 | 画面内の `v0.4.2` 表示削除 | ○ | 画面内の `v0.4.2` 表示が削除された | 継続確認 |
| 4 | グループ参照検索 | ✕ | 対象が表示されず 0 件になった（初回報告では検索できていた） | グループ検索の検索ベースをユーザー OU ではなくドメイン全体に戻して再確認する |
| 5 | ユーザー検索入力の `Mail` | ✕ | `Mail` 条件は不要 | ユーザー検索条件から `Mail` 有無フィルターを削除する |
| 6 | ユーザー検索結果の `Title` | ✕ | `Title` 列は不要 | ユーザー検索結果と CSV から `Title` を削除する |
| 7 | ユーザー検索結果・コンピュータ参照結果の編集可否 | ✕ | 一覧上で編集できるためユーザーが勘違いする | ユーザー・コンピュータ検索結果 DataGrid を読み取り専用・選択のみへ変更する |
| 8 | ServiceMode 選択画面の `EditorAuthMode` 表示 | ✕ | `EditorAuthMode` が出てこない | 起動画面に `EditorAuthMode` と `AdminGroupDn` の読み込み値を表示する |
| 9 | ログ確認の詳細 JSON 表示 | ✕ | ログ検索でヒットしているが「詳細（JSON）」に何も表示されない | 読み込み・フィルター後に先頭行を選択し、詳細 JSON を自動表示する |

---

## 4回目報告の評価

| # | 該当手順・観点 | 結果 | 報告内容 | 対応・次回確認事項 |
|---|---|---|---|---|
| 1 | グループ参照検索 | ○ | グループ参照の検索結果が表示された | 継続確認 |
| 2 | ユーザー検索の `Mail` / `Title` 削除・編集不可 | ○ | ユーザー検索の NG が解消された | 継続確認 |
| 3 | グループ参照のユーザーメンバー表示 | ✕ | ユーザーメンバーが表示されない | グループの `member` 属性から直接メンバー DN を読み取り、ユーザー/コンピュータ/グループを分類して表示する |
| 4 | 起動画面の表示 | ✕ | 表示項目が増えた結果、見切れてしまう | 起動画面を大きくし、リサイズ可能・スクロール可能にする |
| 5 | ログ確認 | ✕ | 表示結果が 1 件しか出てこない。JSON 表示なら一覧画面と詳細画面がほしい | ログ一覧領域を広げ、詳細領域と分割表示する。フィルターは Raw JSON も対象にする |
| 6 | GPOシミュレーションコピー | 要望 | CSV出力と同じ結果の「簡易コピー」がほしい | CSV と同じ列・順序のタブ区切りコピーを追加する |

---

## 5回目報告の評価

| # | 該当手順・観点 | 結果 | 報告内容 | 対応・次回確認事項 |
|---|---|---|---|---|
| 1 | 起動画面の表示 | ○ | 表示項目見切れが解消された | 継続確認 |
| 2 | ログ画面 | ○ | 一覧と詳細に分割された | 継続確認 |
| 3 | グループ参照のユーザーメンバー表示 | ✕ | 引き続きユーザーメンバーが表示されない。ネストグループや所属グループは表示される | `member` の range retrieval と `primaryGroupID` 検索を追加して再確認する |
| 4 | 画面中部タブ領域 | 要望 | 大部分が見切れている。ユーザー検索を「ユーザー詳細」タブへ取り込み、画面の大部分をタブ、下部を処理結果欄の2欄構成にしたい | ルート画面を「編集者ログイン / タブ / 処理結果」の3段に再構成し、ユーザー検索・属性編集をユーザー詳細タブ内へ移動する |

---

## 6回目報告の評価

| # | 該当手順・観点 | 結果 | 報告内容 | 対応・次回確認事項 |
|---|---|---|---|---|
| 1 | グループ参照のユーザーメンバー表示 | ○ | グループ参照のユーザーメンバーが表示された | 継続確認 |
| 2 | UI 変更による見切れ改善 | ○ | 画面見切れの多くが解消された | 継続確認 |
| 3 | 未ログイン対象確認 | 要望 | XX日以上ログインしていないユーザーまたはコンピュータの一覧表示と CSV 出力がほしい | 「未ログイン確認」タブを追加し、日数・対象種別を指定して一覧表示/CSV出力できるようにする |

---

## v0.9.6 修正要望に対する確認項目

| # | 確認項目 | 期待結果 | 結果 | 備考 |
|---|---|---|---|---|
| 1 | リリース ZIP のルート直下 | `ManageAdTool.exe` / `appsettings.json` / `README.md` / `config-samples/` / `docs/` が中心で、大量の DLL が直下に並ばない | ○ | 2回目報告で成功 |
| 2 | 起動時 ServiceMode 選択 | `appsettings.json` を編集せず、起動時ダイアログで `InMemory` / `DirectoryReadOnly` を選択できる | ○ | 2回目報告で選択画面表示は成功。選択後遷移は修正後に再確認 |
| 3 | ユーザー検索条件 | 検索入力欄に「部署」が表示されない | - | v0.9.6 で再確認する |
| 4 | ユーザー検索結果 | 検索結果一覧に `Department` 列が表示されない | - | v0.9.6 で再確認する |
| 5 | 属性表示エリア | 「v0.4.2 更新対象外」などバージョン固有の不要表記が表示されない | ○ | 3回目報告で成功 |
| 6 | ユーザー検索入力 | `Mail` 有無フィルターが表示されない | - | 修正後に再確認 |
| 7 | ユーザー検索結果 | `Title` 列が表示されない | - | 修正後に再確認 |
| 8 | ユーザー・コンピュータ検索結果 | 一覧は編集不可で、選択のみできる | - | 修正後に再確認 |
| 9 | ServiceMode 選択画面 | `EditorAuthMode` が表示される | - | 修正後に再確認 |
| 10 | ログ確認 | 検索ヒット時に「詳細（JSON）」が表示される | - | 修正後に再確認 |
| 11 | グループ詳細 | グループ選択後にユーザーメンバーが表示される | - | 修正後に再確認 |
| 12 | 起動画面 | 表示項目が見切れない | - | 修正後に再確認 |
| 13 | GPOシミュレーション | CSV と同じ列・順序の「簡易コピー」ができる | - | 修正後に再確認 |
| 14 | 画面構成 | ユーザー検索がユーザー詳細タブ内にあり、タブ領域が画面の大部分を占める | ○ | 6回目報告で多くの見切れが解消 |
| 15 | 未ログイン確認 | XX日以上ログインしていないユーザー/コンピュータを一覧表示・CSV出力できる | - | 修正後に再確認 |

---

## 問題・指摘事項

| # | 内容 | 対応方針 | 状態 |
|---|---|---|---|
| 1 | フォルダ直下に大量の DLL ファイル等があり、分かりづらい | self-contained 単一 exe publish に変更し、リリース検証でルート直下 DLL を検出したら失敗させる | v0.9.6 で確認 |
| 2 | `ServiceMode` は `appsettings.json` への記入ではなく、起動時に選択したい | 起動時 ServiceMode 選択ダイアログを追加 | v0.9.6 で確認 |
| 3 | 検索入力欄の「部署」は不要 | ユーザー検索条件から「部署」入力欄を削除 | v0.9.6 で確認 |
| 4 | 検索結果の `Department` は不要 | ユーザー検索結果等の `Department` 列を削除 | v0.9.6 で確認 |
| 5 | 属性表示の「v0.4.2 更新対象外」は不要 | 属性表示エリアのバージョン固有表記を削除 | v0.9.6 で確認 |
| 6 | グループ参照で所属ユーザーが表示されたか不明 | グループ選択後の `ユーザーメンバー` 一覧を再確認する | 追加確認 |
| 7 | ServiceMode 選択後に次画面へ遷移しない | WPF の ShutdownMode を調整し、メイン画面表示前にアプリが終了しないようにする。起動ログと起動失敗時メッセージも追加する | 3回目報告で成功 |
| 8 | グループ参照検索が 0 件になる | グループ検索の検索ベースをドメイン全体に戻す | 修正後に再確認 |
| 9 | ユーザー検索の `Mail` 条件と `Title` 列が不要 | `Mail` フィルターと `Title` 列を削除する | 修正後に再確認 |
| 10 | ユーザー・コンピュータ検索結果が編集できる | DataGrid を読み取り専用にする | 修正後に再確認 |
| 11 | ServiceMode 選択画面に `EditorAuthMode` が出ない | 起動画面に `EditorAuthMode` / `AdminGroupDn` を表示する | 修正後に再確認 |
| 12 | ログ確認の詳細 JSON が空 | 読み込み・フィルター後に先頭ログを自動選択し詳細 JSON を表示する | 修正後に再確認 |
| 13 | グループ参照のユーザーメンバーが表示されない | グループの `member` 属性から直接メンバー DN を読み取り、ユーザー一覧へ反映する | 修正後に再確認 |
| 14 | 起動画面が見切れる | 起動画面をリサイズ・スクロール可能にする | 修正後に再確認 |
| 15 | ログ確認の一覧が少なく見える | 一覧/詳細の縦分割を広げ、Raw JSON もフィルター対象にする | 修正後に再確認 |
| 16 | GPOシミュレーションに簡易コピーがほしい | CSV と同じ列・順序のタブ区切りコピーを追加する | 修正後に再確認 |
| 17 | グループ参照のユーザーメンバーが引き続き表示されない | `member` の range retrieval と `primaryGroupID` 検索を追加する | 修正後に再確認 |
| 18 | 画面中部のタブが見切れる | ユーザー検索・属性編集をユーザー詳細タブへ移し、ルートを3段構成に整理する | 6回目報告で概ね成功 |
| 19 | XX日以上ログインしていないユーザー/コンピュータを確認したい | 「未ログイン確認」タブを追加し、一覧表示と CSV 出力に対応する | 修正後に再確認 |

---

## 総合判定

- [ ] 合格（全項目 ○ またはスキップ。✕ なし）
- [x] 条件付き合格（グループユーザーメンバー表示と UI 見切れ改善は成功。未ログイン確認タブは再確認）
- [ ] 不合格（重大な問題あり・修正後に再検証が必要）

### 特記事項

初回報告では DirectoryReadOnly 起動、ユーザー検索、属性表示、所属グループ表示、コンピュータ参照は成功。2回目報告では配布フォルダ整理と ServiceMode 選択画面表示は成功したが、選択後にメイン画面へ遷移しない問題が判明した。3回目報告では ServiceMode 選択後のメイン画面表示、部署削除、v0.4.2 表示削除は成功。4回目報告ではグループ検索結果表示とユーザー検索 UI の NG 解消は成功。5回目報告では起動画面見切れとログ一覧/詳細分割は成功。6回目報告ではグループユーザーメンバー表示と UI 見切れ改善の多くが成功。新たに XX日以上ログインしていないユーザー/コンピュータの一覧表示と CSV 出力要望が追加されたため、修正後に重点確認する。
