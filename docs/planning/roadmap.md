# ManageAdTool ロードマップ

## 方針

本ツールは閉域ネットワーク向けの **Active Directory 参照・限定編集支援ツール** として開発しています。

- 参照機能を主目的とし、安全性・シンプルさを優先します
- ユーザー書き込みは mail / displayName / sn / givenName のみに限定します
- コンピュータ書き込みは description のみに限定します（v0.5.0 以降）
- グループ書き込みはユーザーのメンバー追加・削除のみに限定します（v0.6.0 以降）
- パスワードは appsettings.json / ログへの保存を行いません
- v1.0.0 前に新しい AD 更新対象や高リスク操作を追加しない方針を優先します

---

## 現在地（v0.9.5）

- AD ユーザー・グループ・コンピュータの参照
- ユーザー属性限定編集（mail / displayName / sn / givenName）
- コンピュータ description 限定編集
- グループメンバー追加・削除（ユーザーのみ）
- 書き込み監査ログ（write-audit.jsonl）・認証ログ（auth.jsonl）
- **オペレーション支援**（v0.7.0〜）: ユーザー所属変更支援・変更予定サマリー生成・チェックリスト
- **GPOシミュレーション**（v0.8.0〜）: ユーザー/コンピュータ対象の適用予定GPO一覧参照（参照・シミュレーションのみ）
- **ログ確認**（v0.9.0〜）: 参照ログ / 認証ログ / 書き込みログのアプリ内ビューア・フィルタリング・CSV出力
- **AD 接続なし単体テスト**（v0.9.3〜）: 設定読み込み、編集可否、ログ読み込み、監査ログ形式、ChangeSet、InMemory サービスを検証
- **操作説明書・運用手順整備**（v0.9.4）: 利用者向け操作説明書、管理者向け設定手順、トラブルシューティング、operation 配下の案内整理
- **リリースパッケージ・配布物の最終整理**（v0.9.5）: self-contained ZIP、README / docs / config-samples / appsettings.json 同梱、配布用 appsettings.json の安全側初期値を整理

v0.9.5 は新しい AD 更新操作を追加せず、閉域端末へ持ち込みやすいリリース ZIP と配布手順を整えるバージョンです。実 AD 環境での動作検証は引き続き `docs/operation/validation-*.md` に従って実施してください。

---

## v1.0.0 までの予定

### v0.9.6: v1.0.0 Release Candidate

- 既知の制約一覧の確定
- validation 結果の最終確認
- v1.0.0 release-note 草案
- バージョン表記確認
- 配布 ZIP の実機展開確認
- 重大な不具合修正のみを対象とする安定化

### v1.0.0: 初回正式リリース

- 閉域環境向けの初回正式リリース
- 利用者向け操作説明書、管理者向け手順、検証記録を同梱
- 新しい AD 更新対象は追加せず、既存の限定編集範囲で安定運用を開始

---

## v1.0.0 後に検討する項目

- 実運用結果に基づく UI 表示・文言の改善
- DirectoryReadOnly 実運用後のキャッシュ評価
  - ユーザー選択切替ごとの `memberOf` 再取得削減
  - セッション内メモリキャッシュ（TTL: 30〜120秒）
- `accountExpires` 更新設計継続（`docs/design/design-account-expiration.md` 参照）
- ユーザー更新対象属性の追加検討（要件定義・安全設計が前提）
- ViewModel 等の追加単体テスト拡充（WPF UI 自動操作ではなく、ロジック単位）

---

## 対象外（今後も実装しない）

- グループ作成・削除・リネーム
- グループをグループに追加
- コンピュータをグループに追加
- GPO 編集・GPO リンク変更・セキュリティフィルタ変更・WMI フィルタ変更
- OU 移動
- ユーザー無効化・退職処理
- パスワードリセット
- 新規ユーザー作成
- サービスアカウント方式（パスワードを設定ファイルに保存する方式）
- 一括更新
- ログ編集・ログ削除・ログ改ざん検知
- ログローテーション・外部 DB 保存
