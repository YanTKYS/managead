# ManageAdTool ロードマップ

## 方針

本ツールは閉域ネットワーク向けの **Active Directory 参照・限定編集支援ツール** として開発しています。

- 参照機能を主目的とし、安全性・シンプルさを優先します
- ユーザー書き込みは mail / displayName / sn / givenName のみに限定します
- コンピュータ書き込みは description のみに限定します（v0.5.0 以降）
- グループ書き込みはユーザーのメンバー追加・削除のみに限定します（v0.6.0 以降）
- パスワードは appsettings.json / ログへの保存を行いません

---

## 現在地（v0.9.1）

- AD ユーザー・グループ・コンピュータの参照
- ユーザー属性限定編集（mail / displayName / sn / givenName）
- コンピュータ description 限定編集
- グループメンバー追加・削除（ユーザーのみ）
- 書き込み監査ログ（write-audit.jsonl）・認証ログ（auth.jsonl）
- **オペレーション支援**（v0.7.0〜）: ユーザー所属変更支援・変更予定サマリー生成・チェックリスト
- **GPOシミュレーション**（v0.8.0〜）: ユーザー/コンピュータ対象の適用予定GPO一覧参照（参照・シミュレーションのみ）
- **ログ確認**（v0.9.0〜）: 参照ログ / 認証ログ / 書き込みログのアプリ内ビューア・フィルタリング・CSV出力
- **安定化**（v0.9.1〜）: 書き込み監査ログのバージョン更新、ログ読み取りの効率化、UI注意文の整理、エラー表示の統一、設定サンプルの整合修正

v0.9.1 は新しい AD 更新操作を追加しない安定化バージョンです。v1.0.0 に向けた実運用準備として位置付けています。実AD環境での動作検証が必要です（`docs/planning/backlog.md` 参照）。

---

## v1.0.0 候補（優先度順・未確定）

v1.0.0 は機能追加より**既存機能の安定化・実運用準備**を主目的とします。v1.0.0 前に新しい AD 更新対象属性を増やさない方針を候補として検討しています。

1. **実AD検証結果の反映**
   - v0.8.x / v0.9.x の実AD検証で判明した問題の修正
   - 各検証手順（validation-*.md）の確認項目消化

2. **UIの最終整理**
   - ボタン配置・ラベル・警告文の見直し
   - 長いディスエーブル理由の表示改善

3. **リリースパッケージ整理**
   - self-contained ZIP の配布手順確認
   - `config-samples/` の最終確認

4. **操作説明書整備**
   - 利用者向けの簡易操作ガイドの追加・整理

5. **既存機能の安定化**
   - キャッシュ評価（DirectoryReadOnly 実運用後）
   - `accountExpires` 更新設計継続（`docs/design/design-account-expiration.md` 参照）

6. **編集機能の拡張**（実運用評価後に検討）
   - ユーザー更新対象属性の追加（要件定義次第・v1.0.0 前は追加しない方針を優先）

---

## 対象外（今後も実装しない）

- グループ作成・削除・リネーム
- グループをグループに追加
- コンピュータをグループに追加
- GPO 編集・GPO リンク変更・セキュリティフィルタ変更・WMI フィルタ変更
- OU 移動
- ユーザー無効化・退職処理
- パスワードリセット
- 新規ユーザー作成
- サービスアカウント方式（パスワードを設定ファイルに保存する方式）
- 一括更新
