# ユーザー属性限定編集 検証手順（v0.4.0）

本手順は **v0.4.0 の限定編集機能**（mail / department / title のAD更新）を  
実 AD 環境で検証するためのものです。

> **重要**: 本機能は mail / department / title のみを対象とします。  
> グループ操作・OU移動・ユーザー無効化・パスワードリセットは実装していません。  
> 更新前に必ず検証用OUと検証用ユーザーを準備してください。

---

## 1. 事前条件

- 閉域端末に ManageAdTool v0.4.0 の成果物が配置済みであること
- 実 AD へ接続可能なネットワーク疎通があること
- 検証用OU（例: `OU=TestUsers,OU=Validation,DC=example,DC=local`）が用意されていること
- 検証用ユーザー（上記OUに所属）が1件以上用意されていること
- Domain Admins アカウント（検証用）が用意されていること
- 検証後に元の属性値に戻す手順を把握していること

---

## 2. appsettings.json の設定

```json
{
  "AppPolicy": {
    "ServiceMode": "DirectoryReadOnly",
    "EditorAuthMode": "DomainAdmins",
    "AdminGroupDn": "CN=Domain Admins,CN=Users,DC=example,DC=local",
    "AllowNestedAdminGroupMembership": false,
    "EditSessionMinutes": 15,
    "AllowedTargetOuDns": [
      "OU=TestUsers,OU=Validation,DC=example,DC=local"
    ],
    "ExcludedSamAccountNames": [
      "administrator",
      "krbtgt"
    ],
    "EditableAttributes": ["mail", "department", "title"],
    "LogPath": "C:\\ProgramData\\ManageAdTool\\logs\\audit.jsonl",
    "MaxSearchResults": 200
  }
}
```

---

## 3. 正常系確認

### 3-1. 検証OU内ユーザーの属性更新

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 1 | アプリ起動 | 正常起動 | | |
| 2 | Domain Admins アカウントでログイン | ログイン成功 | | |
| 3 | 検証用ユーザーを検索・選択 | 詳細が表示される | | |
| 4 | mail を変更して「差分確認」ボタン押下 | 差分が処理結果欄に表示される | | |
| 5 | 「限定更新実行」ボタンが有効になる | 有効 | | 差分確認後のみ有効 |
| 6 | 「限定更新実行」ボタン押下 → 再認証ダイアログが表示される | 表示あり | | |
| 7 | Domain Admins アカウントで再認証 | 再認証成功 | | |
| 8 | 実行前確認ダイアログが表示される | 表示あり | | SamAccountName・DN・変更内容を確認 |
| 9 | 「限定更新実行」ボタンで確定 | AD書き込み実行 | | |
| 10 | 処理結果欄に「更新成功」と対象SamAccountName・DisplayNameが表示される | 表示あり | | |
| 11 | 処理結果欄に属性ごとの「変更前・変更後・AD再取得」が表示される | 表示あり | | |
| 12 | 処理結果欄に「戻し候補」（変更前の値）が表示される | 表示あり | | |
| 13 | 「戻し用メモをコピー」ボタンが有効になる | 有効 | | 更新成功後のみ |
| 14 | 「戻し用メモをコピー」ボタン押下でクリップボードに戻し候補が格納される | 格納あり | | |
| 15 | ADUC等で対象ユーザーの属性が変わっていることを確認 | 変更あり | | |
| 16 | 属性を元の値に戻す | 元の値に戻る | | 本ツールで再実施 |
| 17 | `write-audit.jsonl` に成功ログが記録される | 記録あり | | |
| 18 | `write-audit.jsonl` にパスワードが含まれていない | 含まれていない | | 重要 |

---

### 3-2. department / title の更新

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 19 | department を変更して更新 | 更新成功 | | |
| 20 | title を変更して更新 | 更新成功 | | |
| 21 | mail / department / title の3属性を同時に変更して更新 | 更新成功 | | |

### 3-3. 差分確認状態の確認

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 22 | ログイン未実施の場合、「限定更新実行」ボタン無効理由が「未ログイン…」と表示される | 表示あり | | |
| 23 | ユーザー選択後・差分確認前は「差分確認ボタンを押してください」と表示される | 表示あり | | |
| 24 | 差分確認後（変更あり）は「限定更新実行」ボタンが有効になる | 有効 | | |
| 25 | 入力値を変更すると「限定更新実行」ボタンが再度無効になる | 無効 | | |

### 3-4. 更新前確認ダイアログの確認

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 26 | 確認ダイアログに対象SamAccountName・DisplayNameが表示される | 表示あり | | |
| 27 | 確認ダイアログに実行端末・アプリ起動ユーザー・編集セッションユーザーが表示される | 表示あり | | |
| 28 | 確認ダイアログの変更内容が変更前（赤背景）・変更後（緑背景）で表示される | 表示あり | | |

---

## 4. 異常系・制限確認

### 4-1. 許可OU外ユーザーは更新できない

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 29 | `AllowedTargetOuDns` 外のOUにいるユーザーを選択 | 詳細表示はされる | | 検索範囲外の場合は検索自体がヒットしない |
| 30 | 差分確認後に「限定更新実行」を押下 | 「対象ユーザーが許可OU外のため更新できません」と表示 | | |

### 4-2. ExcludedSamAccountNames のユーザーは更新できない

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 31 | 除外アカウントを検索（`AllowedTargetOuDns` を広げて確認） | 詳細表示はされる | | |
| 32 | 「限定更新実行」を押下 | 「除外アカウントのため更新できません」と表示 | | |

### 4-3. 空文字更新は禁止

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 33 | mail を空文字にして「限定更新実行」を押下 | 「空文字への更新は禁止されています」と表示 | | |
| 34 | ADの属性は変更されていない | 変更なし | | |

### 4-4. AllowedTargetOuDns が空の場合

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 35 | `AllowedTargetOuDns: []` に設定して起動 | 正常起動 | | |
| 35a | ボタン無効理由に「AllowedTargetOuDns 未設定」と表示される | 表示あり | | |
| 36 | 「限定更新実行」を押下 | 「AllowedTargetOuDns が未設定のため更新できません」と表示 | | |

### 4-5. セッション期限切れ時に更新できない

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 37 | `EditSessionMinutes: 1` に設定してログイン | 1 分のセッション | | テスト用 |
| 38 | 1 分経過後に「限定更新実行」ボタンが無効になる | 無効 | | タイマー 30 秒周期 |
| 38a | ボタン無効理由に「セッション期限切れ」と表示される | 表示あり | | |

### 4-6. 非Domain Adminsでは更新できない

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 39 | 非Domain Admins アカウントでログイン試行 | 「ログイン失敗: ～はメンバーではありません」と表示 | | |
| 40 | セッションが開始されない | 開始されない | | |

### 4-7. 再認証失敗時は更新しない

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 41 | 「限定更新実行」押下後の再認証ダイアログで誤パスワードを入力 | 「再認証に失敗しました」と表示 | | |
| 42 | ADの属性は変更されていない | 変更なし | | |

### 4-8a. セッションユーザーと再認証ユーザーの不一致

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 43 | セッションユーザーと異なるアカウントで再認証を試みる | 「再認証ユーザーと編集セッションユーザーが一致しません」と表示 | | 別の Domain Admins アカウントで再認証した場合 |
| 44 | ADの属性は変更されていない | 変更なし | | |
| 45 | `auth.jsonl` に `ReAuthUserMismatch` が記録される | 記録あり | | session / reauth のユーザー名が記録される |

### 4-8b. AD値変更検出（整合性確認）

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 46 | 差分確認後、別ツールで同じユーザーの mail を変更する | | | シミュレーション |
| 47 | 「限定更新実行」を実行 | 「AD上の値が変更されているため更新を中止しました。再度「差分確認」から実行してください。」と表示 | | |
| 48 | `write-audit.jsonl` に失敗ログが記録される | 記録あり | | |

---

## 5. 監査ログ確認

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 49 | 更新成功後 `write-audit.jsonl` が存在する | 存在する | | `LogPath` の同ディレクトリ |
| 50 | `operationName` が `"UpdateUserAttributes"` | 正しい | | |
| 51 | `targetDisplayName` に対象ユーザーの DisplayName が記録される | 正しい | | v0.4.1 新規 |
| 52 | `editorUser` に認証した Domain Admins ユーザーが記録される | 正しい | | |
| 53 | `executor` にアプリ起動ユーザー（`DOMAIN\user`）が記録される | 正しい | | |
| 54 | `changes` に変更前後の値が記録される | 正しい | | |
| 55 | `verifiedAfterUpdate` に更新後AD再取得値が記録される | 正しい | | |
| 56 | `revertCandidate` に変更前の値が記録される | 正しい | | v0.4.1 新規 |
| 57 | `password` フィールドが存在しない | 存在しない | | 重要 |
| 58 | 更新失敗時は `success: false` と `error` が記録される | 正しい | | |
| 59 | 監査ログ保存に失敗した場合（ディスク満杯等）、処理結果欄に「⚠ write-audit.jsonl への保存に失敗しました」と表示される | 表示あり | | 更新自体は成功として扱われる |
| 60 | 監査ログ保存失敗時、`auth.jsonl` に `WriteAuditSaveFailed` が記録される | 記録あり | | ディスク満杯等の状況でのみ発生 |

## 6. ログパス書き込み権限確認

| # | 確認項目 | 期待値 | 結果 | 備考 |
|---|---|---|---|---|
| 61 | LogPath のディレクトリに書き込み権限がない状態で起動 | 起動時に警告メッセージが OutputBox に表示される | | |
| 62 | 参照機能（検索・詳細表示）は正常に動作する | 動作する | | |
| 63 | 書き込み権限なしの状態で「限定更新実行」を押下 | 「監査ログ警告」ダイアログが表示される | | |
| 64 | ダイアログで「いいえ」を選択 | 更新が中止される | | |

---

## 7. 判定基準

- 検証OU内ユーザーの mail / department / title 更新が成功すること
- 許可OU外・除外アカウント・空文字で更新が拒否されること
- セッション期限切れ・未ログインで更新ボタンが無効になり、無効理由が表示されること
- 再認証ダイアログが表示され、失敗時は更新が実行されないこと
- 再認証ユーザーとセッションユーザーが不一致の場合に更新が中止されること
- 更新成功後に変更前・変更後・AD再取得値が表示されること
- 「戻し候補」が表示され、「戻し用メモをコピー」ボタンで取得できること
- `write-audit.jsonl` に `targetDisplayName` と `revertCandidate` が記録されること
- `write-audit.jsonl` にパスワードが記録されないこと
- 監査ログに成功・失敗が正しく記録されること

---

## 8. 注意事項

- 本手順では検証用OUの検証用ユーザーのみを対象にしてください
- 更新後は必ず元の値に戻してください（「戻し用メモをコピー」で取得した値を使用）
- `write-audit.jsonl` は機密情報（ユーザー属性の変更前後）を含むため、適切に管理してください
- パスワードはダイアログ入力後すぐに使用・破棄されます。ログ・設定ファイルには記録されません
- 監査ログディレクトリの書き込み権限は検証前に確認してください
